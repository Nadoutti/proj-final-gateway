version: "3.9"

networks:
  zambom-net:
    external: true

services:
  gateway:
    build: .
    container_name: zambom-gateway
    env_file:
      - .env
    ports:
      - "${APP_PORT:-8080}:8080"
    networks:
      - zambom-net
    restart: unless-stopped
